import styled from 'styled-components';

const StyledGrid = styled.div`
  display: flex;
  justify-content: center;
  position: relative;
  width: 100%;
  flex-flow: row wrap;
  margin: 20px 0;
`;

export { StyledGrid };
